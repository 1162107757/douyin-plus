# 隔空手势刷短视频工具——产品需求文档

> 版本：v0.2（MVP）  
> 平台：Android 优先  
> 状态：双向切换需求已纳入

## 1. 产品概述

开发一款 Android 辅助控制工具。用户先在本工具内启动“隔空控制”，再切换或一键进入抖音；工具在本机使用前置摄像头识别手部运动。当识别到用户完成一次明确的向上或向下挥动时，向当前抖音页面发送对应方向的滑动手势：向上挥动切换到下一个视频，向下挥动返回上一个视频。

产品定位是用户主动开启、状态可见、随时可停的单人辅助控制工具，不做无人值守刷视频、批量账号操作、广告点击或其他自动化行为。

## 2. 可行性结论

Android 版本可实现，推荐技术路径为：

1. CameraX 从前置摄像头持续获取低分辨率画面；
2. MediaPipe Hand Landmarker/Gesture Recognizer 在设备端提取手部关键点；
3. 手势状态机根据掌心或腕部轨迹判定一次有效的上挥或下挥，并锁定动作方向；
4. Android AccessibilityService 的 `dispatchGesture()` 模拟一次对应方向的屏幕滑动；
5. 相机识别运行在带常驻通知的 camera 类型前台服务中。

必须注意：Android 新版本要求相机前台服务在应用界面可见时由用户主动启动，之后再进入抖音；运行期间系统会显示相机隐私指示和前台服务通知。不同品牌手机的后台保活策略也会影响稳定性。

iOS 不纳入本期。标准 iOS 应用无法可靠地向另一个应用注入滑动手势；不考虑越狱或私有 API 方案。

## 3. 产品目标

### 3.1 MVP 目标

- 用户不触碰屏幕，通过一次自然、明确的向上挥手切换到下一个视频，或向下挥手返回上一个视频；
- 全部视觉数据在本机处理，不保存、不上传摄像头画面；
- 正常室内光照下具有可接受的识别速度、准确率和功耗；
- 权限、运行状态和停止入口清晰可见，避免静默采集或误操作；
- 控制动作仅在抖音前台时生效，不影响其他应用。

### 3.2 非目标

- 不识别身份、人脸、表情或情绪；
- 不读取或分析抖音视频内容；
- 不自动点赞、评论、关注、私信或发布内容；
- 不记录账号行为，不做云端画像；
- 不保证在直播、拍摄、视频通话等其他应用占用摄像头的场景可用；
- MVP 不支持 iOS、PC、抖音极速版及其他短视频应用。

## 4. 目标用户与典型场景

### 4.1 目标用户

- 做饭、健身、吃东西等双手不便触屏时想切换视频的用户；
- 手机放在支架上、距离屏幕约 40～120 cm 使用的用户；
- 有触控不便、希望以手势替代触屏操作的用户。

### 4.2 主要使用流程

1. 用户首次打开工具，阅读隐私说明；
2. 用户依次授予相机权限、通知权限，并按引导开启辅助功能服务；
3. 用户进入校准页，确认前摄能够识别手掌；
4. 用户点击“开始隔空控制”；
5. 工具启动前台识别服务并显示常驻通知；
6. 用户点击“打开抖音”或手动切换到抖音；
7. 用户先展示张开的手掌，再向上或向下挥动；
8. 工具锁定手势方向，识别成功后轻微振动，并发送一次同方向滑动；
9. 用户通过通知中的“暂停/停止”按钮结束识别。

## 5. 功能需求

### 5.1 P0：MVP 必须实现

| 编号 | 功能 | 需求说明 |
| --- | --- | --- |
| FR-01 | 首次使用引导 | 解释工作原理、相机用途、辅助功能用途和运行限制；未完成必要权限时不得进入工作状态。 |
| FR-02 | 相机授权 | 仅请求前置摄像头权限；拒绝后展示原因和系统设置入口。 |
| FR-03 | 辅助功能引导 | 跳转系统辅助功能设置页，并在返回后检查服务状态；不得声称普通弹窗授权即可完成。 |
| FR-04 | 开始控制 | 必须由用户在本工具可见界面点击启动；启动成功后展示明确的“运行中”状态。 |
| FR-05 | 前摄取帧 | 使用前置摄像头低分辨率取帧，默认不显示、保存或上传图像；分析跟不上时丢弃旧帧、保留最新帧。 |
| FR-06 | 手部检测 | 支持单手检测，输出至少 21 个手部关键点、置信度和时间戳。 |
| FR-07 | 双向手势识别 | 识别“张开手掌准备 → 在限定时间内明显向上或向下移动 → 完成”的连续动作，不以单帧姿势直接触发。 |
| FR-08 | 上挥切换下一个 | 每次有效上挥只派发一次从屏幕下部到上部的滑动，从而切换到下一个视频；动作时长和距离需适配不同屏幕尺寸。 |
| FR-09 | 下挥返回上一个 | 每次有效下挥只派发一次从屏幕上部到下部的滑动，从而返回上一个视频；动作时长和距离需适配不同屏幕尺寸。 |
| FR-10 | 应用范围限制 | 仅当抖音包处于活动窗口时允许派发动作；离开抖音立即停止触发，但识别服务可保持待机。 |
| FR-11 | 方向锁定与防误触 | 候选动作超过方向阈值后锁定为上挥或下挥；同一次轨迹中途反向不得触发第二个动作。 |
| FR-12 | 防重复与复位 | 触发后进入冷却期；必须等手部回到中性区域或离开画面后才可再次触发。 |
| FR-13 | 状态反馈 | 识别成功时提供可关闭的短振动反馈，并显示“下一个”或“上一个”的方向结果；运行期间显示不可静默隐藏的前台服务通知。 |
| FR-14 | 暂停与停止 | 应用主页及常驻通知均提供暂停/继续/停止；停止后立即释放摄像头、模型和前台服务。 |
| FR-15 | 本地隐私与异常处理 | 不录制视频、不截屏、不保存摄像头帧；相机被占用、辅助功能关闭、服务被系统终止或模型初始化失败时给出明确提示。日志只能记录事件时间、方向、识别耗时、置信度和成功/失败原因。 |

### 5.2 P1：首个可用版本建议实现

| 编号 | 功能 | 需求说明 |
| --- | --- | --- |
| FR-16 | 双向识别校准 | 分别引导用户完成一次向上挥动和一次向下挥动；实时显示“未检测到手/准备就绪/方向已锁定/动作过快/动作幅度不足”等状态。 |
| FR-17 | 灵敏度设置 | 提供低、中、高三档；上下方向默认共用灵敏度，同时保留独立阈值的内部调试能力。 |
| FR-18 | 悬浮状态点 | 经用户额外授权后，在抖音上方显示一个小状态点：灰色暂停、绿色就绪、蓝色下一个、橙色上一个；状态同时配合箭头，不只依赖颜色。 |
| FR-19 | 仅前台包白名单 | 用户可从受支持应用列表中开启抖音；MVP 默认只包含抖音。 |
| FR-20 | 诊断信息导出 | 用户主动操作时，可导出不含图片的设备型号、系统版本、性能、识别方向与错误日志。 |

### 5.3 P2：后续候选

- 手势自定义与个体化校准；
- 握拳暂停、张掌继续；
- 横屏与平板适配；
- 无障碍用户专用的大字体与语音反馈；
- 在合规评估后增加其他短视频应用。

## 6. 双向手势判定规则（初始建议值）

手势识别采用状态机，参数需在真机测试后调整：

1. **空闲**：未检测到稳定手掌；
2. **准备**：单只张开手掌连续稳定出现约 200～400 ms；
3. **追踪**：以掌心中心（或腕部与掌指关节中心）作为轨迹点；
4. **方向锁定**：净垂直位移达到画面高度约 6% 时，根据轨迹方向锁定为上挥或下挥；锁定后，同一次候选动作不允许改变方向；
5. **有效上挥**：在约 200～800 ms 内向上移动不少于画面高度的 18%，且垂直位移至少为水平位移的 1.5 倍；
6. **有效下挥**：在约 200～800 ms 内向下移动不少于画面高度的 18%，且垂直位移至少为水平位移的 1.5 倍；
7. **触发**：关键点平均置信度达到阈值后，上挥派发一次屏幕上滑，下挥派发一次屏幕下滑；
8. **冷却**：默认 1.0～1.5 秒，期间不接受任何方向的新动作；
9. **复位**：手回到画面中性区域或离开画面后重新进入空闲状态。

动作与结果映射：

| 用户手势 | 系统模拟手势 | 抖音结果 |
| --- | --- | --- |
| 手掌向上挥动 | 屏幕由下向上滑动 | 切换到下一个视频 |
| 手掌向下挥动 | 屏幕由上向下滑动 | 返回上一个视频 |

补充规则：

- 坐着轻微调整姿势、拿杯子、摸脸、从画面中伸手进入等动作不得直接触发；
- 两只手同时出现时，MVP 只跟踪置信度最高且最稳定的一只手；
- 上挥后立即反向下挥但尚未完成复位时，不得被识别为第二个动作；下挥后的处理相同；
- 画面过暗、关键点跳变或手部被遮挡时取消本次候选动作；
- 前摄镜像只影响预览，不得导致上下方向判断反转。

## 7. 非功能需求与验收指标

### 7.1 性能

- 推荐分析输入：约 640×480，15～20 FPS；按设备性能动态降帧；
- 从完成有效上挥或下挥到系统开始滑动：P95 不高于 500 ms；
- 连续运行 30 分钟无崩溃、无摄像头资源泄漏；
- 中端 Android 真机连续运行时不得出现明显阻塞抖音播放的卡顿；
- 高温或低电量时允许自动降低分析帧率，并提示用户。

### 7.2 准确性

在正常室内光照、手机固定、距离 40～120 cm 的测试条件下：

- 100 次规范上挥和 100 次规范下挥，各方向成功识别率均不低于 92%；
- 30 分钟正常观看但无目标手势，误触发不超过 1 次；
- 上下方向误判率不高于 1%，上挥不得触发返回、下挥不得触发下一条；
- 单次手势不得产生连续两次滑动；
- 戴眼镜、口罩或画面中有人脸不应影响结果，因为系统不使用人脸数据。

### 7.3 隐私与安全

- 所有推理默认离线执行；
- 不申请麦克风、通讯录、存储、位置等无关权限；
- 前摄使用期间必须保持系统相机指示和常驻通知；
- 隐私说明应明确：采集目的、处理位置、是否保存、停止方式；
- 辅助功能服务只执行白名单应用中的预定义滑动，不读取输入框、密码、聊天内容或页面文本；
- 不提供隐藏运行、开机自启刷视频、定时刷视频等能力。

### 7.4 兼容性

- MVP 建议最低支持 Android 10，目标 SDK 使用开发时最新稳定版本；
- 至少测试 Android 原生/Pixel、小米、OPPO、vivo、荣耀各一台设备；
- 覆盖全面屏、不同分辨率、60/90/120 Hz 刷新率；
- 抖音更新导致页面结构变化时，上滑和下滑模拟仍应基于相对屏幕坐标工作，而非依赖页面控件 ID。

## 8. 权限清单

| 权限/能力 | 是否必须 | 用途 |
| --- | --- | --- |
| Camera | 是 | 获取前摄画面并在本机识别手势。 |
| Accessibility Service | 是 | 在抖音前台时根据识别方向派发一次系统上滑或下滑。 |
| Foreground Service / Camera 类型 | 是 | 用户进入抖音后继续进行可见的相机分析。 |
| Notifications | Android 版本相关 | 展示运行状态以及暂停、停止操作。 |
| Vibration | 否 | 手势识别成功反馈。 |
| Display over other apps | 否，P1 | 展示悬浮状态点；拒绝后不影响核心功能。 |
| Internet | MVP 不需要 | 端侧模型随安装包发布，不上传数据。 |

## 9. 页面与交互

MVP 包含四个页面：

1. **隐私与引导页**：解释权限和数据处理；
2. **主页**：权限状态、开始/暂停/停止、打开抖音、当前识别状态；
3. **校准页**：前摄预览、手部骨架、上下方向练习、距离与动作反馈；预览画面只存在于内存；
4. **设置页**：双向手势开关、灵敏度、振动反馈、隐私说明、诊断日志。上挥和下挥在 MVP 中默认均开启。

主页核心状态：未配置、可启动、启动中、运行中、已暂停、异常、已停止。

## 10. 技术模块建议

- `camera`：CameraX 生命周期、前摄取帧、方向与镜像处理；
- `vision`：MediaPipe 模型加载、关键点输出、性能降级；
- `gesture`：轨迹平滑、上下方向锁定、状态机、阈值、冷却与复位；
- `control`：AccessibilityService、抖音包名白名单、上下方向映射与相对坐标滑动；
- `service`：camera 类型前台服务、通知操作、资源释放；
- `ui`：引导、主页、校准、设置与诊断；
- `privacy`：无图像持久化约束、日志脱敏与清理。

## 11. 风险与限制

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 厂商后台策略终止服务 | 进入抖音后识别停止 | 前台服务、明确提示，并为常见厂商提供电池优化说明。 |
| 光照差、手太近或太远 | 漏识别或误识别 | 校准页、质量检测、动态阈值和清晰的失败反馈。 |
| 上下方向误判或快速反向产生重复动作 | 返回错误视频或一次跳过两条 | 方向锁定、单次轨迹单一结果、全方向冷却和强制复位。 |
| 长时间前摄推理 | 发热、耗电 | 低分辨率、降帧、只追踪单手、温度降级、暂停入口。 |
| 抖音或其他应用占用摄像头 | 相机打开失败 | 检测占用并暂停，用户离开拍摄/直播页面后重试。 |
| 辅助功能用途与应用商店政策 | 上架审核受限 | 先按本地测试/内部发布验证；公开发布前单独完成商店政策与隐私合规评估。 |
| 抖音平台规则 | 分发或使用风险 | 定位为用户即时辅助操作，不加入无人值守、批量或商业自动化功能。 |

## 12. MVP 验收场景

1. 首次安装后，未授权相机或未开启辅助功能时无法误进入运行态；
2. 用户从主页启动后进入抖音，常驻通知和相机系统指示可见；
3. 在推荐视频页完成一次规范上挥，只切换到下一个视频；
4. 在推荐视频页完成一次规范下挥，只返回到上一个视频；
5. 先上挥、完成复位、再下挥，应先进入下一条再返回原视频；
6. 连续同向挥动两次且中间完成复位，可准确切换两个视频；
7. 单次轨迹先上后下或先下后上但未复位时，只能产生第一次锁定方向的一个动作；
8. 挥手过小、横向挥动、摸脸或拿杯子不会触发；
9. 切换到微信、桌面或系统设置后，即使挥手也不会产生滑动；
10. 点击通知“暂停”后摄像头停止或进入明确的低功耗暂停态，且不再触发；
11. 点击“停止”后前台服务、摄像头和模型资源全部释放；
12. 断网状态下完整功能仍可使用；
13. 检查应用数据目录，不存在摄像头图片或视频文件。

## 13. 待产品确认

以下三项会影响下一步原型与开发计划：

1. 首发是否确定为 **Android 手机**；
2. 第一版已确定包含 **向上挥动切换下一个视频** 和 **向下挥动返回上一个视频**；暂不加入点赞、关注等内容操作；
3. 交付目标是 **个人安装/内部测试 APK**，还是计划公开上架应用商店。

建议默认选择：Android、MVP 同时支持上挥与下挥、先以个人/内部 APK 验证。开发验证时先完成上挥闭环，再复用同一状态机加入下挥，并重点测试方向误判和快速反向动作。
